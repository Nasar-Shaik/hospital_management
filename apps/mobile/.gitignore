# Expo
.expo/
dist/
web-build/
expo-env.d.ts.bak

# Native projects — generated by `expo prebuild`, never committed (M0 §2: managed workflow)
/ios
/android

# Signing material must never reach the repository
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-services.json
GoogleService-Info.plist
