# apps/web — copy to .env for local dev.
# Server-side only (server components / middleware):
API_BASE_URL=http://localhost:4000
# Client-visible config MUST use NEXT_PUBLIC_ and may never hold secrets (Doc 09 §12):
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
